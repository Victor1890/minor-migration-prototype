---
import ContactInfo from "@/components/ContactInfo/index.astro";
import type { NodeRoute } from "@/data/data-route";

interface Props {
  data: NodeRoute;
}

const { data } = Astro.props;

if('histories' in data === false || !Array.isArray(data.histories)) {
  throw new Error("Just now, we have a problem with the data. Please, try again later.");
}

---

<div class="w-full flex flex-col gap-8">
  <section
    aria-labelledby="info-title"
    class="p-6 rounded-[8px] flex flex-col gap-4 bg-[#EFF7FF]"
  >
    <h2 id="info-title" class="font-semibold text-[18px]">
      Según la información que nos indicaste, el menor:
    </h2>

    <ul
      aria-label="Lista de pasos seleccionados"
      class="list-disc font-semibold text-[16px] ml-6"
    >
      {data?.histories?.map((item) => <li>{item?.label}</li>)}
    </ul>

    <p>Aun así, debes llevar los documentos requeridos para el viaje:</p>
    {
      /* <p>
          En tu caso,{" "}
          <strong>
            no es necesario solicitar el permiso de salida para el menor
          </strong>
          . Aun así, te recomendamos llevar los documentos básicos del menor
          (como pasaporte, acta de nacimiento y visa si corresponde), ya que
          podrían ser solicitados por Migración u otras autoridades al momento
          del viaje.
        </p> */
    }

    <ul
      aria-label="Lista de pasos seleccionados"
      class="list-disc font-semibold text-[16px] ml-6"
    >
      {
        [
          "Pasaporte",
          "Acta de nacimiento",
          "Visa (en caso de que sea necesaria)",
        ].map((item) => <li>{item}</li>)
      }
    </ul>
  </section>

  <ContactInfo />
</div>
